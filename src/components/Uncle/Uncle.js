import React from 'react';

const Uncle = ({house}) => {
    return (
        <div>
            <h1>This is uncle</h1>
            <p>House : {house}</p>
        </div>
    );
};

export default Uncle;